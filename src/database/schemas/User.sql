CREATE TABLE IF NOT EXISTS users (
	username  TEXT NOT NULL PRIMARY KEY,
	password  TEXT NOT NULL,
-- 	potential future components to add to users
-- 	email     TEXT NOT NULL,
-- 	firstName TEXT NOT NULL,
-- 	lastName  TEXT NOT NULL,
-- 	gender    TEXT NOT NULL
) ;